export class register {
    name: string
    username: string
    password: string
    email: string
    timer: number[]
    description: string[]


}

export class timer{
    startTimer: number;
    stopTimer: number;
}